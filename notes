add date validation for birthday entry -- date cannot be in the future.

time validation for nap entry cannot be in the future.

write spec tests

naps should have a baby_id
naps should have a start and end time

babies should be assigned to an owner.

ordered list of naps - naps should be displayed as their own block. also calculate time of nap.